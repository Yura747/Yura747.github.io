
const form = document.querySelector('.form');

const input = [].slice.call(form.elements);
form.addEventListener('submit', function(e) {
  e.preventDefault();
   input.forEach(item => {
     if (!item.value) {
       item.classList.add('error')
     }
   });
});